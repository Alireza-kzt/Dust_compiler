import Nothing
import Nothing2
import OneFunnyClass
import Car

class Human(Nothing, Nothing2){
    int var
    Nose nose
    Hand[2] hands
    Leg[2] legs
    bool isHungry
    bool isHungry
    int isHungry

    def Human(Nose n){
        legs[1] = legs[20]
        self.nose = n
    }

    def Voice speak(float a){
        while(true){
            int hi = 0
        }
        Voic voice
        return voice
    }

    def void eat(Food food, int c){
        int calories = ((voice + x - ww) / e) * c
        while(self.isHungry) {
            Food newFood = Food()
            eat(newFood)
            if(calories > 2) {
                print("hi")
            }
            self.isHungry = self.checkIsHungry(true)
            self.isHungry = self.checkIsNotHun()
        }
        int b = 0
    }

    def bool checkIsHungry() {
        Food food = Food()
        int a = calories
        self.eat(food, b)
        int a = calories + 1
        return 0
    }
}